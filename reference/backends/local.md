# 本地存储

适用于：

- 本机磁盘路径；
- 已通过 NFS / SMB 等方式挂载到服务器的共享目录。

在表单中，本地存储主要需要配置：

- **根路径**（必填）
  - 本地文件系统中的一个目录，例如：`/data/storage`。
  - WANOS 会在该路径下创建并管理自己的数据目录，请确保该目录具有足够权限和空间。

其他通用字段：

- 「副本数」「总容量」的含义与前文一致，可根据实际磁盘和 RAID 情况估算。
